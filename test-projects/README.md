# 🍇 Grape 测试项目

Grape 测试项目目录，用于测试 npm 包管理功能，不影响系统全局配置。

## 📁 项目列表

| 项目 | 说明 | 状态 |
|------|------|------|
| [vue3-demo](./vue3-demo/) | Vue 3 + Vite + TypeScript 基础项目 | ✅ 已创建 |

## 🚀 快速开始

### 1. 启动 Grape 服务

```bash
cd /path/to/grape
./bin/grape
```

### 2. 进入测试项目

```bash
cd test-projects/vue3-demo
```

### 3. 安装依赖

```bash
# 方式一：使用脚本（推荐）
cd test-projects
./install.sh

# 方式二：手动安装
cd test-projects/vue3-demo
npm install
```

### 4. 运行项目

```bash
npm run dev
# 访问 http://localhost:5173
```

## 📋 配置说明

### 项目级 .npmrc

每个测试项目都有自己的 `.npmrc` 文件：

```ini
registry=http://localhost:4873
audit=false
fund=false
```

**优点：**
- ✅ 仅在当前项目生效
- ✅ 不影响全局配置
- ✅ 不影响其他项目
- ✅ 删除项目即可清理

### 目录结构

```
test-projects/
├── README.md              # 本文件
├── 测试指南.md            # 详细测试指南
├── install.sh             # 快速安装脚本
├── .gitignore             # Git 忽略配置
├── vue3-demo/             # Vue 3 测试项目
│   ├── .npmrc             # 项目级 npm 配置
│   ├── package.json       # 项目依赖
│   ├── src/               # 源码
│   └── README.md          # 项目说明
└── <future-project>/      # 未来测试项目
```

## 🧪 测试场景

### 公共包安装

```bash
cd test-projects/vue3-demo
npm install lodash
npm install express
```

### Scoped 包安装

```bash
npm install @babel/core
npm install @vueuse/core
```

### 私有包发布

```bash
# 登录
npm login --registry http://localhost:4873

# 发布
npm publish --registry http://localhost:4873
```

### 缓存测试

```bash
# 第一次安装（从上游）
npm install lodash

# 清除并重新安装（从缓存）
rm -rf node_modules
npm install lodash
```

## 📚 文档

- [测试项目总览](./README.md) - 本文件
- [详细测试指南](./测试指南.md) - 完整的测试场景和验证方法
- [vue3-demo 说明](./vue3-demo/README.md) - Vue3 测试项目说明

## 🔧 脚本工具

### install.sh

快速安装脚本，自动检查 Grape 服务并安装依赖：

```bash
./install.sh
```

## ⚠️ 注意事项

1. **先启动 Grape**：确保 Grape 服务正在运行
2. **项目级配置**：配置仅在测试项目内生效
3. **独立依赖**：每个项目有独立的 node_modules
4. **清理简单**：删除目录即可清理

## 🆘 常见问题

### Grape 服务未启动

```bash
# 启动 Grape
./bin/grape

# 验证
curl http://localhost:4873/-/health
```

### 配置未生效

```bash
# 检查配置
npm config list

# 应看到 registry=http://localhost:4873
```

### 需要帮助

查看 [测试指南](./测试指南.md) 或 [Grape 文档](../docs/)

---

🍇 Grape Test Projects - 安全、便捷的 npm 包管理测试环境
